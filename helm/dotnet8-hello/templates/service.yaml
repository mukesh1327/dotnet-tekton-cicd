apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotnet8-hello.fullname" . }}
  labels:
    {{- include "dotnet8-hello.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dotnet8-hello.selectorLabels" . | nindent 4 }}
